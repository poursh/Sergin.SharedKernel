<MudNavMenu>
    @foreach (SerginNavItem item in navItems)
    {
        <MudNavLink Href="@item.Href" Icon="@item.Icon" Match="@MatchFor(item)">
            @Localizer[item.Label]
        </MudNavLink>
    }
</MudNavMenu>
